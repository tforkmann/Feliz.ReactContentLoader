namespace Feliz.ReactContentLoader

open Fable.Core

/// This interface allows us to stop adding random props to the react flow.
type IReactContentLoaderProp = interface end
type IRectProp = interface end
type IStyleProp = interface end
